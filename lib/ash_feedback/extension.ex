defmodule AshFeedback.Extension do
  @moduledoc """
  Optional `Ash.Resource` extension for per-resource feedback links
  (e.g., letting an admin attach a feedback entry to a specific
  resource record).

  Off the critical path for MVP. Lands in Phase 6+.
  """
end
