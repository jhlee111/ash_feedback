defmodule AshFeedback.Domain do
  @moduledoc """
  Example Ash domain wrapping `AshFeedback.Resources.Feedback`. Hosts
  either use this domain directly or alias the resource into one of
  their own domains.
  """

  # Implementation lands in Phase 4a.
end
